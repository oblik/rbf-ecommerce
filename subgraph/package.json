{
  "name": "subgraph",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio rbf-base",
    "create-local": "graph create --node http://localhost:8020/ rbf-base",
    "remove-local": "graph remove --node http://localhost:8020/ rbf-base",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 rbf-base"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.97.1",
    "@graphprotocol/graph-ts": "^0.38.1"
  }
}
