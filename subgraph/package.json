{
  "name": "rbf-ecommerce-subgraph",
  "version": "1.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio rbf-ecommerce",
    "create-local": "graph create --node http://localhost:8020/ rbf-ecommerce",
    "remove-local": "graph remove --node http://localhost:8020/ rbf-ecommerce",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 rbf-ecommerce"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.97.1",
    "@graphprotocol/graph-ts": "^0.38.1"
  }
}
